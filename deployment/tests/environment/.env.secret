# Secrets for fabric-only deployment.
# All fabric component secrets are defined in the .env.onboard.* / .env.pla
# env files (e.g. SomeRandomPassword). This file is kept for structural
# parity with the workspace environments folder.
